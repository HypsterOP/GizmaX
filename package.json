{
  "name": "gizmax",
  "version": "1.0.0",
  "description": "Yet another pog discord bot",
  "main": "src/index.ts",
  "scripts": {
    "test": "ts-node-dev --respawn --transpile-only --poll ./src/index.ts",
    "prettier": "npx prettier write ./",
    "start": "node .",
    "dev": "npm run build && npm start",
    "build": "npx tsc"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^14.14.5",
    "ts-node-dev": "^1.1.8"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "eris": "^0.15.1",
    "typescript": "^4.3.5"
  }
}
